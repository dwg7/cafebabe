# Contributing to cafebabe

## パターンの書き方

各パターンは以下の4項目で書きます(クリストファー・アレグザンダーのパターン・ランゲージを参考にした形式です):

```markdown
## <パターン名(短く、検索しやすく)>

**タグ**: 一般則 | 個別事情(<プロジェクト名>)

**状況(Context)**
このパターンが関係してくる場面。どんなプロジェクトの、どんな構成で問題になるか。

**問題/対立する力(Problem / Forces)**
何が難しいのか。なぜ素朴にやるとうまくいかないのか。

**解決(Solution)**
実際にどう解決したか。コード例・設定値・具体的な回避策を書く。

**実例(Known uses)**
- `プロジェクト名` — 一言でどう使っているか
```

### タグについて

- **一般則**: MapLibre/Open MCT/Gitといった、dwg7に限らずどのプロジェクトでも通用する知見
- **個別事情(プロジェクト名)**: 特定のプロジェクトのハードウェア・データ・技術選定に強く依存する知見

判断に迷ったら「一般則」寄りに倒して構いません。後から他プロジェクトでの実例(Known uses)が
増えれば、それ自体が一般則であることの裏付けになります。

## 訂正の仕方

書いた内容が古くなった・間違っていたと分かっても、**その場で書き換えず**、パターンの末尾に
日付つきの訂正を追記してください:

```markdown
**訂正 (2026-09-15)**: 上記の回避策はv6.2.0で不要になった。詳細は...
```

理由が変わった経緯そのものが、後から同じ勘違いをしそうな人への警告になります
(dwg7の各プロジェクトのDECISIONS.mdで共通して採られている方針です)。

## PRの出し方

1. 該当する`patterns/*.md`が無ければ新規作成(既存ファイルへの追記の方が望ましい場合が多いです)
2. 上記の型でパターンを追記
3. 既存のパターンへの追加なら、`実例(Known uses)`に1行足すだけでも歓迎します
4. PRのタイトルは「add: <パターン名>」または「update: <パターン名> (<プロジェクト名>の実例追加)」のように

## 大きな判断(名前・構成・分割方針など)について

このリポジトリ自体の運用に関わる判断は`DECISIONS.md`にADRとして記録してください。
複数プロジェクトにまたがる話なので、可能であれば他のエージェントにcross-session messageで
一声かけてから進めると良いです。
